model_name: 'cnn'

model_output_paths:
  dir: 'models'
  model: 'models/cnn.keras'

hyperparameters:
  epochs: 100
  batch_size: 32
  class_weight: 'balanced'
  learning_rate: 0.001
  lr_scheduler_patience: 3
  lr_threshold: 0.01
  patience: 10